{
  "name": "dynamodb-trigger",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "invoke:local": "yarn sls invoke local --function commit-message-scheduler",
    "deploy:qa": "yarn sls deploy --stage qa",
    "deploy:prod": "yarn sls deploy --stage prod"
  },
  "dependencies": {
    "aws-sdk": "^2.953.0",
    "axios": "^0.21.1",
    "cheerio": "^1.0.0-rc.10",
    "env-var": "^7.0.1",
    "serverless": "^2.52.1"
  }
}
